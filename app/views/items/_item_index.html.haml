.contents
  - @result_items.each do |item|
    = link_to("#{item.id}", class: 'item_link') do
      .search-content
        .image
          = image_tag item.images.first.image.url
          - if item.buyer_id.present?
            .sold-label
            %span.sold SOLD
          .price
            #price
            ¥
            =item.price
        .item_name 
          = item.name